# RippleRadius
RippleRadius: A pipeline-aware framework for recovering and/or improving multi-stage LLM systems after model migration. Characterizes semantic cascade degradation, estimates downstream impact, and jointly re-optimizes prompts across affected stages.

